spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://warehouse-db:5432/warehouse
    username: warehouse
    password: warehouse
    driver-class-name: org.postgresql.Drive